# Task: greet

Acknowledge that the Verona daemon is alive. Output one line:
`hello from <agent-name> at <ISO timestamp> — task: <task-id>`

Then write a one-paragraph episodic log entry to `memory/learned/episodic/` summarizing the run (timestamp, what triggered, anything notable).
